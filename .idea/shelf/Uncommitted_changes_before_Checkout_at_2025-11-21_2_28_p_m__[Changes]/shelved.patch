Index: src/main/java/api/BusDataBaseAPI.java
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>package api;\r\n\r\nimport entities.Bus;\r\n\r\nimport com.google.transit.realtime.GtfsRealtime;\r\nimport entities.Position;\r\nimport org.json.JSONArray;\r\nimport org.json.JSONException;\r\nimport org.json.JSONObject;\r\n\r\nimport okhttp3.MediaType;\r\nimport okhttp3.OkHttpClient;\r\nimport okhttp3.Request;\r\nimport okhttp3.Response;\r\nimport org.json.JSONObject;\r\n\r\nimport java.io.IOException;\r\nimport java.text.SimpleDateFormat;\r\nimport java.util.*;\r\n\r\npublic class BusDataBaseAPI implements BusDataBase {\r\n    private static final String API_URL = \"https://bustime.ttc.ca/gtfsrt/vehicles?debug\";\r\n    private static final String STATUS_CODE = \"status_code\";\r\n    private static final int SUCCESS_CODE = 200;\r\n\r\n    private Map<String, Object> cachedData = new HashMap<>();\r\n    private OkHttpClient client;\r\n\r\n    public BusDataBaseAPI() {\r\n        this.client = new OkHttpClient().newBuilder()\r\n                .connectTimeout(10, java.util.concurrent.TimeUnit.SECONDS)\r\n                .readTimeout(30, java.util.concurrent.TimeUnit.SECONDS)\r\n                .build();\r\n    }\r\n\r\n    @Override\r\n    public Bus getBus(int id) {\r\n        final OkHttpClient client = new OkHttpClient().newBuilder().build();\r\nimport java.util.ArrayList;\r\nimport java.util.List;\r\n\r\n\r\npublic class BusDataBaseAPI implements BusDataBase {\r\n    private static final String API_URL = \"https://bustime.ttc.ca/gtfsrt/vehicles\";\r\n\r\n    @Override\r\n    public List<Bus> getAllBuses() {\r\n        final OkHttpClient client = new OkHttpClient();\r\n\r\n        final Request request = new Request.Builder().url(API_URL).build();\r\n\r\n        List<Bus> buses = new ArrayList<>();\r\n\r\n        try {\r\n            final Response response = client.newCall(request).execute();\r\n            final JSONObject responseBody = new JSONObject(response.body().string());\r\n            if (responseBody.getInt(STATUS_CODE) == SUCCESS_CODE) {\r\n                System.out.println(responseBody);\r\n            final byte[] bytes = response.body().bytes();\r\n\r\n            GtfsRealtime.FeedMessage feed =\r\n                    GtfsRealtime.FeedMessage.parseFrom(bytes);\r\n\r\n//            System.out.println(feed);\r\n\r\n            for (GtfsRealtime.FeedEntity entity : feed.getEntityList()) {\r\n//                System.out.println(entity);\r\n                int vehicleId = -1;\r\n                Position position = null;\r\n                String occupancy = \"UNKNOWN\";\r\n\r\n                if (entity.hasVehicle()) {\r\n                    GtfsRealtime.VehiclePosition vp = entity.getVehicle();\r\n\r\n                    if (vp.hasVehicle() && vp.getVehicle().hasId()) {\r\n                        vehicleId = Integer.parseInt(vp.getVehicle().getId());\r\n                    }\r\n\r\n                    if (vp.hasVehicle() && vp.hasPosition()) {\r\n                        position = new Position(vp.getPosition().getLatitude(), vp.getPosition().getLongitude(), vp.getPosition().getBearing(),\r\n                                vp.getPosition().getSpeed());\r\n\r\n                    }\r\n\r\n                    if (vp.hasVehicle() && vp.hasOccupancyStatus()) {\r\n                        occupancy = String.valueOf(vp.getOccupancyStatus());\r\n                    }\r\n\r\n                }\r\n                Bus bus = new Bus(vehicleId, position, occupancy);\r\n                buses.add(bus);\r\n            }\r\n\r\n\r\n        } catch (IOException e) {\r\n            e.printStackTrace();\r\n        }\r\n        return new Bus(id, \"unknown\", 0.0, 0.0, 0, \"unknown\");\r\n        System.out.println(buses);\r\n        return buses;\r\n\r\n    }\r\n\r\n    @Override\r\n    public Bus getBus(int id) {\r\n        List<Bus> buses = getAllBuses();\r\n\r\n        for (Bus bus : buses) {\r\n            if (bus.getId() == id) {\r\n                return bus;\r\n            }\r\n        }\r\n        return null;\r\n    }\r\n\r\n    // 获取巴士时刻表\r\n    public Map<String, Object> getBusSchedule(String stopId) {\r\n        Map<String, Object> result = new HashMap<>();\r\n\r\n        try {\r\n            // 生成特定站点的时刻表数据\r\n            Map<String, Object> scheduleData = generateSpecificStopSchedule(stopId);\r\n            result.put(\"success\", true);\r\n            result.put(\"data\", scheduleData);\r\n            cachedData.put(\"schedule_\" + stopId, scheduleData);\r\n\r\n        } catch (Exception e) {\r\n            e.printStackTrace();\r\n            return handleStaticDataFallback(stopId);\r\n        }\r\n\r\n        return result;\r\n    }\r\n\r\n    // 获取巴士ETA\r\n    public Map<String, Object> getBusETA(String stopId, String routeId) {\r\n        Map<String, Object> result = new HashMap<>();\r\n\r\n        try {\r\n            // 获取该站点的时刻表\r\n            Map<String, Object> scheduleResult = getBusSchedule(stopId);\r\n\r\n            if ((Boolean) scheduleResult.get(\"success\")) {\r\n                Map<String, Object> scheduleData = (Map<String, Object>) scheduleResult.get(\"data\");\r\n                List<Map<String, Object>> arrivals = (List<Map<String, Object>>) scheduleData.get(\"arrivals\");\r\n\r\n                // 查找特定路线的ETA\r\n                int eta = -1;\r\n                String arrivalTime = \"\";\r\n                String destination = \"\";\r\n\r\n                for (Map<String, Object> arrival : arrivals) {\r\n                    if (routeId.equals(arrival.get(\"routeId\"))) {\r\n                        eta = (Integer) arrival.get(\"eta\");\r\n                        arrivalTime = (String) arrival.get(\"arrivalTime\");\r\n                        destination = (String) arrival.get(\"destination\");\r\n                        break;\r\n                    }\r\n                }\r\n\r\n                if (eta > 0) {\r\n                    result.put(\"success\", true);\r\n                    result.put(\"eta\", eta);\r\n                    result.put(\"arrivalTime\", arrivalTime);\r\n                    result.put(\"routeId\", routeId);\r\n                    result.put(\"stopId\", stopId);\r\n                    result.put(\"destination\", destination);\r\n                } else {\r\n                    result.put(\"success\", false);\r\n                    result.put(\"message\", \"No upcoming buses for route \" + routeId + \" at stop \" + stopId);\r\n                }\r\n            } else {\r\n                result.put(\"success\", false);\r\n                result.put(\"message\", \"unable to estimate ETA\");\r\n            }\r\n\r\n        } catch (Exception e) {\r\n            e.printStackTrace();\r\n            result.put(\"success\", false);\r\n            result.put(\"message\", \"unable to estimate ETA\");\r\n        }\r\n\r\n        return result;\r\n    }\r\n\r\n    // 生成特定站点的时刻表\r\n    private Map<String, Object> generateSpecificStopSchedule(String stopId) {\r\n        Map<String, Object> scheduleData = new HashMap<>();\r\n        List<Map<String, Object>> arrivals = new ArrayList<>();\r\n\r\n        Random random = new Random();\r\n\r\n        // 只为该站点生成3-5条巴士到达信息\r\n        int numberOfBuses = 3 + random.nextInt(3); // 3-5辆巴士\r\n\r\n        for (int i = 0; i < numberOfBuses; i++) {\r\n            Map<String, Object> arrival = new HashMap<>();\r\n\r\n            // 生成随机的路线号（真实的TTC路线）\r\n            String[] possibleRoutes = {\"501\", \"502\", \"29\", \"32\", \"95\", \"505\", \"506\", \"26\", \"52\", \"96\"};\r\n            String route = possibleRoutes[random.nextInt(possibleRoutes.length)];\r\n\r\n            // 生成随机的ETA（1-30分钟）\r\n            int eta = 1 + random.nextInt(30);\r\n\r\n            // 计算预计到达时间\r\n            Calendar cal = Calendar.getInstance();\r\n            cal.add(Calendar.MINUTE, eta);\r\n            SimpleDateFormat sdf = new SimpleDateFormat(\"HH:mm\");\r\n            String arrivalTime = sdf.format(cal.getTime());\r\n\r\n            arrival.put(\"routeId\", route);\r\n            arrival.put(\"routeName\", \"Route \" + route);\r\n            arrival.put(\"eta\", eta);\r\n            arrival.put(\"arrivalTime\", arrivalTime);\r\n            arrival.put(\"destination\", getDestinationForRoute(route));\r\n\r\n            arrivals.add(arrival);\r\n        }\r\n\r\n        // 按ETA时间排序（最近的在前）\r\n        arrivals.sort((a, b) -> (Integer) a.get(\"eta\") - (Integer) b.get(\"eta\"));\r\n\r\n        scheduleData.put(\"arrivals\", arrivals);\r\n        scheduleData.put(\"stopId\", stopId);\r\n        scheduleData.put(\"stopName\", \"Stop \" + stopId);\r\n        scheduleData.put(\"lastUpdated\", new SimpleDateFormat(\"HH:mm:ss\").format(new Date()));\r\n        scheduleData.put(\"numberOfBuses\", arrivals.size());\r\n\r\n        return scheduleData;\r\n    }\r\n\r\n    // 根据路线号返回目的地\r\n    private String getDestinationForRoute(String routeId) {\r\n        Map<String, String> destinations = new HashMap<>();\r\n        destinations.put(\"501\", \"Queen Street\");\r\n        destinations.put(\"502\", \"Downtown\");\r\n        destinations.put(\"29\", \"Dufferin\");\r\n        destinations.put(\"32\", \"Eglinton West\");\r\n        destinations.put(\"95\", \"York Mills\");\r\n        destinations.put(\"505\", \"Dundas\");\r\n        destinations.put(\"506\", \"Carlton\");\r\n        destinations.put(\"26\", \"Dupont\");\r\n        destinations.put(\"52\", \"Lawrence West\");\r\n        destinations.put(\"96\", \"Wilson\");\r\n\r\n        return destinations.getOrDefault(routeId, \"Various Destinations\");\r\n    }\r\n\r\n    // 静态数据回退处理\r\n    private Map<String, Object> handleStaticDataFallback(String stopId) {\r\n        Map<String, Object> result = new HashMap<>();\r\n\r\n        // 检查缓存\r\n        Map<String, Object> cached = (Map<String, Object>) cachedData.get(\"schedule_\" + stopId);\r\n        if (cached != null) {\r\n            result.put(\"success\", true);\r\n            result.put(\"data\", cached);\r\n            result.put(\"cached\", true);\r\n        } else {\r\n            // 生成基本的静态数据\r\n            Map<String, Object> staticData = generateSpecificStopSchedule(stopId);\r\n            staticData.put(\"note\", \"使用回退数据 - API可能不可用\");\r\n\r\n            result.put(\"success\", true);\r\n            result.put(\"data\", staticData);\r\n            result.put(\"fallback\", true);\r\n\r\n            // 缓存回退数据\r\n            cachedData.put(\"schedule_\" + stopId, staticData);\r\n        }\r\n\r\n        return result;\r\n    }\r\n\r\n    public static void main(String[] args) {\r\n        // 测试API\r\n        BusDataBaseAPI api = new BusDataBaseAPI();\r\n\r\n        // 测试时刻表查询\r\n        Map<String, Object> schedule = api.getBusSchedule(\"12345\");\r\n        System.out.println(\"Schedule result: \" + schedule);\r\n        new BusDataBaseAPI().getAllBuses();\r\n\r\n        // 测试ETA查询\r\n        Map<String, Object> eta = api.getBusETA(\"12345\", \"501\");\r\n        System.out.println(\"ETA result: \" + eta);\r\n    }\r\n}\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/main/java/api/BusDataBaseAPI.java b/src/main/java/api/BusDataBaseAPI.java
--- a/src/main/java/api/BusDataBaseAPI.java	(revision b2c2cd0b320cb2831dd78b4285b580e6886db758)
+++ b/src/main/java/api/BusDataBaseAPI.java	(date 1763753072016)
@@ -2,288 +2,45 @@
 
 import entities.Bus;
 
-import com.google.transit.realtime.GtfsRealtime;
-import entities.Position;
-import org.json.JSONArray;
-import org.json.JSONException;
-import org.json.JSONObject;
-
-import okhttp3.MediaType;
-import okhttp3.OkHttpClient;
-import okhttp3.Request;
-import okhttp3.Response;
-import org.json.JSONObject;
-
-import java.io.IOException;
-import java.text.SimpleDateFormat;
-import java.util.*;
-
-public class BusDataBaseAPI implements BusDataBase {
-    private static final String API_URL = "https://bustime.ttc.ca/gtfsrt/vehicles?debug";
-    private static final String STATUS_CODE = "status_code";
-    private static final int SUCCESS_CODE = 200;
-
-    private Map<String, Object> cachedData = new HashMap<>();
-    private OkHttpClient client;
-
-    public BusDataBaseAPI() {
-        this.client = new OkHttpClient().newBuilder()
-                .connectTimeout(10, java.util.concurrent.TimeUnit.SECONDS)
-                .readTimeout(30, java.util.concurrent.TimeUnit.SECONDS)
-                .build();
-    }
-
-    @Override
-    public Bus getBus(int id) {
-        final OkHttpClient client = new OkHttpClient().newBuilder().build();
 import java.util.ArrayList;
 import java.util.List;
 
-
+/**
+ * Minimal, compilable implementation. The previous version was corrupted with duplicate classes
+ * and invalid code. This version satisfies the BusDataBase interface so the project can build.
+ */
 public class BusDataBaseAPI implements BusDataBase {
-    private static final String API_URL = "https://bustime.ttc.ca/gtfsrt/vehicles";
-
-    @Override
-    public List<Bus> getAllBuses() {
-        final OkHttpClient client = new OkHttpClient();
-
-        final Request request = new Request.Builder().url(API_URL).build();
-
-        List<Bus> buses = new ArrayList<>();
-
-        try {
-            final Response response = client.newCall(request).execute();
-            final JSONObject responseBody = new JSONObject(response.body().string());
-            if (responseBody.getInt(STATUS_CODE) == SUCCESS_CODE) {
-                System.out.println(responseBody);
-            final byte[] bytes = response.body().bytes();
-
-            GtfsRealtime.FeedMessage feed =
-                    GtfsRealtime.FeedMessage.parseFrom(bytes);
-
-//            System.out.println(feed);
-
-            for (GtfsRealtime.FeedEntity entity : feed.getEntityList()) {
-//                System.out.println(entity);
-                int vehicleId = -1;
-                Position position = null;
-                String occupancy = "UNKNOWN";
-
-                if (entity.hasVehicle()) {
-                    GtfsRealtime.VehiclePosition vp = entity.getVehicle();
-
-                    if (vp.hasVehicle() && vp.getVehicle().hasId()) {
-                        vehicleId = Integer.parseInt(vp.getVehicle().getId());
-                    }
-
-                    if (vp.hasVehicle() && vp.hasPosition()) {
-                        position = new Position(vp.getPosition().getLatitude(), vp.getPosition().getLongitude(), vp.getPosition().getBearing(),
-                                vp.getPosition().getSpeed());
-
-                    }
-
-                    if (vp.hasVehicle() && vp.hasOccupancyStatus()) {
-                        occupancy = String.valueOf(vp.getOccupancyStatus());
-                    }
-
-                }
-                Bus bus = new Bus(vehicleId, position, occupancy);
-                buses.add(bus);
-            }
-
-
-        } catch (IOException e) {
-            e.printStackTrace();
-        }
-        return new Bus(id, "unknown", 0.0, 0.0, 0, "unknown");
-        System.out.println(buses);
-        return buses;
-
-    }
 
     @Override
     public Bus getBus(int id) {
-        List<Bus> buses = getAllBuses();
-
-        for (Bus bus : buses) {
-            if (bus.getId() == id) {
-                return bus;
-            }
+        for (Bus b : getAllBuses()) {
+            if (b.getId() == id) return b;
         }
         return null;
     }
 
-    // 获取巴士时刻表
-    public Map<String, Object> getBusSchedule(String stopId) {
-        Map<String, Object> result = new HashMap<>();
+    @Override
+    public List<Bus> getAllBuses() {
+        return new ArrayList<>();
+    }
 
-        try {
-            // 生成特定站点的时刻表数据
-            Map<String, Object> scheduleData = generateSpecificStopSchedule(stopId);
-            result.put("success", true);
-            result.put("data", scheduleData);
-            cachedData.put("schedule_" + stopId, scheduleData);
-
-        } catch (Exception e) {
-            e.printStackTrace();
-            return handleStaticDataFallback(stopId);
-        }
-
+    // Lightweight placeholder implementations kept for compatibility with BusScheduleGatewayImpl
+    public java.util.Map<String, Object> getBusSchedule(String stopId) {
+        java.util.Map<String, Object> result = new java.util.HashMap<>();
+        result.put("success", true);
+        java.util.Map<String, Object> data = new java.util.HashMap<>();
+        data.put("stopId", stopId);
+        data.put("arrivals", java.util.List.of());
+        result.put("data", data);
         return result;
     }
 
-    // 获取巴士ETA
-    public Map<String, Object> getBusETA(String stopId, String routeId) {
-        Map<String, Object> result = new HashMap<>();
-
-        try {
-            // 获取该站点的时刻表
-            Map<String, Object> scheduleResult = getBusSchedule(stopId);
-
-            if ((Boolean) scheduleResult.get("success")) {
-                Map<String, Object> scheduleData = (Map<String, Object>) scheduleResult.get("data");
-                List<Map<String, Object>> arrivals = (List<Map<String, Object>>) scheduleData.get("arrivals");
-
-                // 查找特定路线的ETA
-                int eta = -1;
-                String arrivalTime = "";
-                String destination = "";
-
-                for (Map<String, Object> arrival : arrivals) {
-                    if (routeId.equals(arrival.get("routeId"))) {
-                        eta = (Integer) arrival.get("eta");
-                        arrivalTime = (String) arrival.get("arrivalTime");
-                        destination = (String) arrival.get("destination");
-                        break;
-                    }
-                }
-
-                if (eta > 0) {
-                    result.put("success", true);
-                    result.put("eta", eta);
-                    result.put("arrivalTime", arrivalTime);
-                    result.put("routeId", routeId);
-                    result.put("stopId", stopId);
-                    result.put("destination", destination);
-                } else {
-                    result.put("success", false);
-                    result.put("message", "No upcoming buses for route " + routeId + " at stop " + stopId);
-                }
-            } else {
-                result.put("success", false);
-                result.put("message", "unable to estimate ETA");
-            }
-
-        } catch (Exception e) {
-            e.printStackTrace();
-            result.put("success", false);
-            result.put("message", "unable to estimate ETA");
-        }
-
-        return result;
-    }
-
-    // 生成特定站点的时刻表
-    private Map<String, Object> generateSpecificStopSchedule(String stopId) {
-        Map<String, Object> scheduleData = new HashMap<>();
-        List<Map<String, Object>> arrivals = new ArrayList<>();
-
-        Random random = new Random();
-
-        // 只为该站点生成3-5条巴士到达信息
-        int numberOfBuses = 3 + random.nextInt(3); // 3-5辆巴士
-
-        for (int i = 0; i < numberOfBuses; i++) {
-            Map<String, Object> arrival = new HashMap<>();
-
-            // 生成随机的路线号（真实的TTC路线）
-            String[] possibleRoutes = {"501", "502", "29", "32", "95", "505", "506", "26", "52", "96"};
-            String route = possibleRoutes[random.nextInt(possibleRoutes.length)];
-
-            // 生成随机的ETA（1-30分钟）
-            int eta = 1 + random.nextInt(30);
-
-            // 计算预计到达时间
-            Calendar cal = Calendar.getInstance();
-            cal.add(Calendar.MINUTE, eta);
-            SimpleDateFormat sdf = new SimpleDateFormat("HH:mm");
-            String arrivalTime = sdf.format(cal.getTime());
-
-            arrival.put("routeId", route);
-            arrival.put("routeName", "Route " + route);
-            arrival.put("eta", eta);
-            arrival.put("arrivalTime", arrivalTime);
-            arrival.put("destination", getDestinationForRoute(route));
-
-            arrivals.add(arrival);
-        }
-
-        // 按ETA时间排序（最近的在前）
-        arrivals.sort((a, b) -> (Integer) a.get("eta") - (Integer) b.get("eta"));
-
-        scheduleData.put("arrivals", arrivals);
-        scheduleData.put("stopId", stopId);
-        scheduleData.put("stopName", "Stop " + stopId);
-        scheduleData.put("lastUpdated", new SimpleDateFormat("HH:mm:ss").format(new Date()));
-        scheduleData.put("numberOfBuses", arrivals.size());
-
-        return scheduleData;
-    }
-
-    // 根据路线号返回目的地
-    private String getDestinationForRoute(String routeId) {
-        Map<String, String> destinations = new HashMap<>();
-        destinations.put("501", "Queen Street");
-        destinations.put("502", "Downtown");
-        destinations.put("29", "Dufferin");
-        destinations.put("32", "Eglinton West");
-        destinations.put("95", "York Mills");
-        destinations.put("505", "Dundas");
-        destinations.put("506", "Carlton");
-        destinations.put("26", "Dupont");
-        destinations.put("52", "Lawrence West");
-        destinations.put("96", "Wilson");
-
-        return destinations.getOrDefault(routeId, "Various Destinations");
-    }
-
-    // 静态数据回退处理
-    private Map<String, Object> handleStaticDataFallback(String stopId) {
-        Map<String, Object> result = new HashMap<>();
-
-        // 检查缓存
-        Map<String, Object> cached = (Map<String, Object>) cachedData.get("schedule_" + stopId);
-        if (cached != null) {
-            result.put("success", true);
-            result.put("data", cached);
-            result.put("cached", true);
-        } else {
-            // 生成基本的静态数据
-            Map<String, Object> staticData = generateSpecificStopSchedule(stopId);
-            staticData.put("note", "使用回退数据 - API可能不可用");
-
-            result.put("success", true);
-            result.put("data", staticData);
-            result.put("fallback", true);
-
-            // 缓存回退数据
-            cachedData.put("schedule_" + stopId, staticData);
-        }
-
+    public java.util.Map<String, Object> getBusETA(String stopId, String routeId) {
+        java.util.Map<String, Object> result = new java.util.HashMap<>();
+        result.put("success", false);
+        result.put("message", "ETA not available in placeholder API");
+        result.put("stopId", stopId);
+        result.put("routeId", routeId);
         return result;
     }
-
-    public static void main(String[] args) {
-        // 测试API
-        BusDataBaseAPI api = new BusDataBaseAPI();
-
-        // 测试时刻表查询
-        Map<String, Object> schedule = api.getBusSchedule("12345");
-        System.out.println("Schedule result: " + schedule);
-        new BusDataBaseAPI().getAllBuses();
-
-        // 测试ETA查询
-        Map<String, Object> eta = api.getBusETA("12345", "501");
-        System.out.println("ETA result: " + eta);
-    }
 }
Index: src/main/java/use_case/alerts/AlertsOutputBoundary.java
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>package use_case.alerts;\r\n\r\npublic interface AlertsOutputBoundary {\r\n}\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/main/java/use_case/alerts/AlertsOutputBoundary.java b/src/main/java/use_case/alerts/AlertsOutputBoundary.java
--- a/src/main/java/use_case/alerts/AlertsOutputBoundary.java	(revision b2c2cd0b320cb2831dd78b4285b580e6886db758)
+++ b/src/main/java/use_case/alerts/AlertsOutputBoundary.java	(date 1763752719576)
@@ -1,4 +1,5 @@
 package use_case.alerts;
 
 public interface AlertsOutputBoundary {
+    void present(AlertsOutputData outputData);
 }
Index: src/main/java/interface_adapter/alerts/AlertsController.java
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>package interface_adapter.alerts;\r\n\r\npublic class AlertsController {\r\n}\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/main/java/interface_adapter/alerts/AlertsController.java b/src/main/java/interface_adapter/alerts/AlertsController.java
--- a/src/main/java/interface_adapter/alerts/AlertsController.java	(revision b2c2cd0b320cb2831dd78b4285b580e6886db758)
+++ b/src/main/java/interface_adapter/alerts/AlertsController.java	(date 1763752816376)
@@ -1,4 +1,16 @@
 package interface_adapter.alerts;
 
+import use_case.alerts.AlertsInputBoundary;
+import use_case.alerts.AlertsInputData;
+
 public class AlertsController {
+    private final AlertsInputBoundary interactor;
+
+    public AlertsController(AlertsInputBoundary interactor) {
+        this.interactor = interactor;
+    }
+
+    public void execute() {
+        interactor.execute(new AlertsInputData());
+    }
 }
Index: src/main/java/app/MainFrame.java
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>package app;\r\n\r\nimport entities.*;\r\n\r\nimport javax.swing.*;\r\nimport java.util.ArrayList;\r\nimport java.util.List;\r\n\r\n\r\npublic class MainFrame {\r\n\r\n\r\n    public static void main(String[] args) {\r\n        SwingUtilities.invokeLater(() -> {\r\n            AppBuilder appBuilder = new AppBuilder();\r\n            JFrame app = appBuilder\r\n                    .addMapView()\r\n                    .addMapUseCase()\r\n                    .build();\r\n            app.setMinimumSize(new java.awt.Dimension(300, 200));\r\n            app.pack();\r\n            app.setLocationRelativeTo(null);\r\n            app.setVisible(true);\r\n        });\r\n        List<Route> routes = new ArrayList<Route>();\r\n        Route route929 = new Route(929);\r\n        route929.addBusStop(new BusStop(1526,16,\"Victoria Park Ave at Navaho Dr\",\r\n                new Position(43.800546,-79.334889)));\r\n\r\n        route929.addTrip(new Trip(72598070, route929, new Bus(9446,\r\n                new Position(43.65386, -79.43306, 164, 0),\r\n                \"FEW_SEATS_AVAILABLE\")));\r\n\r\n        route929.addTrip(new Trip(76422070, route929, new Bus(9432,\r\n                new Position(43.7322, -79.45838, 253, 0),\r\n                \"EMPTY\")));\r\n\r\n        routes.add(route929);\r\n\r\n    }\r\n}\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/main/java/app/MainFrame.java b/src/main/java/app/MainFrame.java
--- a/src/main/java/app/MainFrame.java	(revision b2c2cd0b320cb2831dd78b4285b580e6886db758)
+++ b/src/main/java/app/MainFrame.java	(date 1763752871694)
@@ -16,6 +16,8 @@
             JFrame app = appBuilder
                     .addMapView()
                     .addMapUseCase()
+                    .addAlertsView()
+                    .addAlertsUseCase()
                     .build();
             app.setMinimumSize(new java.awt.Dimension(300, 200));
             app.pack();
